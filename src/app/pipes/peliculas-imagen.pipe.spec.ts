import { PeliculasImagenPipe } from './peliculas-imagen.pipe';

describe('PeliculasImagenPipe', () => {
  it('create an instance', () => {
    const pipe = new PeliculasImagenPipe();
    expect(pipe).toBeTruthy();
  });
});
